import { useState, forwardRef } from 'react'
import { Menu } from 'antd'

export type MemoreMenuProps = {}
export type MemoreMenuItemProps = {}

const MemoreMenuItem = forwardRef<HTMLElement, MemoreMenuItemProps>(
  (props, ref) => {
    return (
      <></>
    )
  },
)

const MemoreMenu = forwardRef<HTMLElement, MemoreMenuProps>(
  (props, ref) => {
    return (
      <></>
    )
  }
)